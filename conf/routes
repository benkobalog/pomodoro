# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# WebPage endpoints
GET     /                controllers.HomeController.index
POST    /                controllers.HomeController.userForm

# Pomodoro RPC style endpoints
POST   /pomodoroStart     controllers.DataController.pomodoroStart
PATCH  /pomodoroFinish    controllers.DataController.pomodoroFinish
GET    /pomodoro          controllers.DataController.pomodoroGet
GET    /pomodoroState     controllers.DataController.pomodoroState

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
